version: 0.1
application:
  name: Periodic Table
  ports:
    - port: 80
      service: true
      ingress: true
      ingressPath: /
build:
  commands:
    - cmd: pip3
      args:
        - install
        - Kivy
        - kivymd
exec:
  commands:
    - cmd: cat
      args:
        - README
    - cmd: /docker-entrypoint.sh
      args:
        - nginx
        - '-g'
        - daemon off;
